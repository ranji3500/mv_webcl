import {
  DocumentCategory,
  OrderStage,
  OrderStatus,
} from "../enums/common.enums";

export type TOrderStatus = `${OrderStatus}`;
export type TDocumentCategory = `${DocumentCategory}`;
export type TOrderStage = `${OrderStage}`;
